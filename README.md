# myportal

Небольшой сайт на Go для выдачи доступа к резюме: скачивание PDF возможно **только один раз** по одноразовой ссылке. В письме приходит код подтверждения (чтобы убедиться, что указан реальный email). Есть админка: кто запросил резюме и кто скачал, кто нет.

## Запуск

```bash
cp .env.example .env
# заполнить RESUME_PATH (путь к PDF), ADMIN_SECRET (пароль админки), при необходимости SMTP

go run .
```

Сервер слушает порт из `PORT` (по умолчанию 8080).

## Docker

```bash
docker build -t myportal .
docker run -p 8080:8080 \
  -e ADMIN_SECRET=secret \
  -e RESUME_PATH=/data/resume.pdf \
  -v /path/to/your/resume.pdf:/data/resume.pdf \
  -v myportal-data:/app/data \
  myportal
```

Переменные окружения задаются через `-e` или `--env-file` (файл не должен попадать в репозиторий). Том `myportal-data` сохраняет заявки между перезапусками.

## Сборка на GitHub

В репозитории настроен GitHub Actions (`.github/workflows/build.yml`): при push и pull request в `main`/`master` запускаются сборка Go и сборка Docker-образа. Файлы `.env` и `data/` в репозиторий не попадают (см. `.gitignore` и `.dockerignore`).

## Переменные окружения

| Переменная     | Описание |
|----------------|----------|
| `PORT`         | Порт (по умолчанию 8080) |
| `BASE_URL`     | Базовый URL сайта для ссылок в письме |
| `RESUME_PATH`  | Путь к файлу резюме (PDF). Раздача только по одноразовой ссылке. |
| `ADMIN_SECRET` | Пароль входа в админку `/admin` |
| `SMTP_*`       | SMTP для отправки писем (если не заданы — ссылка и код в лог) |

## Сценарий

1. Пользователь оставляет email и принимает [Terms of Use](/terms).
2. На email уходит письмо с **кодом подтверждения** (чтобы убедиться, что адрес реальный) и ссылкой для перехода на страницу скачивания.
3. По ссылке открывается страница с **одноразовой ссылкой на PDF**. Скачать файл можно только один раз; повторный переход по ссылке скачивания выдаёт ошибку.
4. **Админка** `/admin` (пароль — `ADMIN_SECRET`): таблица заявок — email, дата запроса, подтвердил ли email, скачал ли файл.


# 📄 License

**No License** - All Rights Reserved

This code is proprietary software. You may view this code for portfolio 
demonstration and evaluation purposes only. Any use, copying, modification, 
or distribution requires explicit written permission.

For licensing inquiries, please contact: igor@trubnikov.family
